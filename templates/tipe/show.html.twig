{% extends 'base.html.twig' %}

{% block title %}Tipe{% endblock %}

{% block body %}
    <h1>Tipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipe.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ tipe.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipe_index') }}">back to list</a>

    <a href="{{ path('tipe_edit', {'id': tipe.id}) }}">edit</a>

    {{ include('tipe/_delete_form.html.twig') }}
{% endblock %}
